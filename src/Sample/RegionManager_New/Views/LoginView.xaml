<UserControl
    x:Class="RegionManager_Version2.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RegionManager_Version2.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Border
            Width="360"
            Padding="30"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="{StaticResource CardBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="10">

            <StackPanel>

                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="22"
                        FontWeight="Bold"
                        Text="Welcome" />
                    <TextBlock Foreground="Gray" Text="Please sign in to continue" />
                </StackPanel>

                <StackPanel Margin="0,16">
                    <TextBox Margin="0,0,0,10" Text="{Binding UserId}" />
                    <PasswordBox Height="36" />
                </StackPanel>

                <Button
                    Command="{Binding LoginCommand}"
                    Content="LOGIN"
                    Style="{StaticResource PrimaryButtonStyle}" />

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
