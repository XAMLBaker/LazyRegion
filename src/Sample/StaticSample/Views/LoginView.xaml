<UserControl
    x:Class="StaticSample.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="500"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Border
            Width="360"
            Padding="30"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="{StaticResource CardBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="10">
            <StackPanel>
                <TextBlock
                    FontSize="22"
                    FontWeight="Bold"
                    Text="🔑 Login" />
                <TextBlock
                    Margin="0,4,0,20"
                    Foreground="Gray"
                    Text="Navigate here via LazyRegionApp.Default" />

                <TextBlock
                    Margin="0,0,0,4"
                    FontWeight="SemiBold"
                    Text="Username" />
                <TextBox
                    Margin="0,0,0,12"
                    Height="36"
                    Padding="8,0"
                    VerticalContentAlignment="Center" />

                <TextBlock
                    Margin="0,0,0,4"
                    FontWeight="SemiBold"
                    Text="Password" />
                <PasswordBox
                    Margin="0,0,0,24"
                    Height="36" />

                <Button
                    Click="GoToHome_Click"
                    Content="← Back to Home"
                    Style="{StaticResource PrimaryButtonStyle}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
